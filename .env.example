# Server-only Supabase credentials. Never expose the service role key through a VITE_ variable.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<service-role-secret>

# Groble payment setup
GROBLE_PAYMENT_URL=https://www.groble.im/payment/4SGBV5
# Set only after Groble documents its exact signed-header and signature algorithm.
GROBLE_WEBHOOK_SECRET=<groble-webhook-secret>
# Register this deployed endpoint for Groble's "일반결제 완료" test send.
# https://passmate-gamma.vercel.app/api/webhooks/groble
