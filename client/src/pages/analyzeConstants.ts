// 서버 normalizeRequest(api/analyze.js)와 같은 상한. 한쪽만 바꾸지 않는다.
export const MAX_QUESTIONS = 5;
export const MAX_TOTAL_CHARS = 6000;
export const MIN_TOTAL_CHARS = 200;
export const WARN_TOTAL_CHARS = 1000;
