/** 사이트 식별자. seo.ts 와 structuredData.ts 가 함께 쓰므로 둘 사이 순환 import 를 피해 따로 둔다. */
export const SITE_ORIGIN = "https://pre-view.me";
export const SITE_NAME = "Pre:View";
export const DEFAULT_OG_IMAGE = `${SITE_ORIGIN}/og-image.png`;
export const SITE_LOGO = `${SITE_ORIGIN}/pre-view-wordmark.png`;
